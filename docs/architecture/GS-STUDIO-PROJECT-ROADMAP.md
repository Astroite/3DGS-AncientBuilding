# GS-Studio 项目路线图

更新：2026-09-23。状态：正式全流程 GUI 已有实现代码，运行验收仍暂停。本文件是后续项目排期与状态的现行入口；工程拆分见 [工程路线图](GS-STUDIO-ENGINEERING-ROADMAP.md)。当前实际操作仍以 [Windows 工作手册](../user/CURRENT-WORKFLOW.md)为准。

## 已确认方向与现状

- GSStudio 保留仓库和目录，产品名统一为 **GS-Studio**。日常项目操作最终从 GUI 完成；`gsstudio` CLI 保留给脚本、恢复和底层操作，业务规则由两种入口共用。
- GUI 覆盖地点、场景、Capture、Run、重建、QA、训练、模型编辑与交付。首次安装、底层诊断和应急恢复仍可使用脚本或 CLI。
- 正式主线沿用现有 Python/gsstudio/gsplat 基础。**新 GUI 实验预选 gsplat**，并可明确选择 Postshot；现行 CLI `train --segment` 的 Postshot 默认与 Run 配置不因路线图而改变。旧包名和旧命令无兼容入口；现行 Run 清单 schema 与 Data 布局保持不变。
- [A 深色专业工作台](../archive/GS-Studio-Plan/design/specification-a/README.md)已通过静态设计评审，覆盖重建后工作台；采集、Run、重建和 QA 的 GUI 流程设计仍需补齐。GSStudio 的 Tk Studio 是功能原型，不是正式 UI 或运行验收结论。
- [GS-Studio-Plan 历史资料](../archive/GS-Studio-Plan/README.md)保留 PRD、需求编号、设计稿、技术评估与历史证据。其 2026-09-13 的下游工具范围和 LichtFeld 主推荐均作为当时记录；本文件及[工程路线图](GS-STUDIO-ENGINEERING-ROADMAP.md)承载 2026-09-22 后的新方向。

## 需求与验收追踪

旧需求 I（导入）、T（训练）、V（查看）、E（编辑）、O（交付）、UI 和 NFR 编号继续有效于其原有功能。新增上游需求仅作为路线图编号，实施前在统一需求清单中展开，不修改数据或清单格式。

| 新编号 | GUI 目标 | 计划验收 |
| --- | --- | --- |
| C-01 | 浏览并创建地点、场景、Capture；显示已存在对象并拒绝覆盖 | AC-17 |
| C-02 | 从 GUI 探测输入、展示来源和前置条件，再执行现行 ingest | AC-17 |
| R-01 | 创建新 Run、呈现配置与身份、启动或恢复 preprocess | AC-18 |
| R-02 | 遮罩预览、人工门禁、重建与一次受限 repair 的状态和操作 | AC-18 |
| Q-01 | 查看分段适用性与路线覆盖，按实际选中结果选择可训练分段 | AC-19 |
| Q-02 | 展示 Run/训练证据与清理预览，确认后按现行白名单执行 | AC-19 |
| G-01 | GUI 项目浏览、依赖检查、日志、等待/失败/恢复与当前动作提示 | AC-20 |

AC-17：创建和探测流程保持原片只读、不覆盖已有清单；输入异常可定位。AC-18：GUI 只允许满足现行阶段前置条件的操作，状态、锁、输入哈希和恢复身份与 Run 清单一致。AC-19：分段可训练与全路线覆盖分别显示；清理先预览，执行仍受 Run 锁、白名单及消费者校验约束。AC-20：项目浏览和错误信息可定位到实际清单、日志及结果；等待、失败或仅有输出文件均不显示为成功。这些编号是未来验收场景，当前均未执行。

## 里程碑与出口

| 阶段 | 依赖 | 主要交付 | 退出条件与追踪 |
| --- | --- | --- | --- |
| P0 路线图整合（本轮） | APP 现行手册、旧计划与用户确定的方向 | 本文件、工程路线图、双向索引与历史状态说明 | 静态核对入口覆盖、链接、默认值和证据措辞；不标记功能验收 |
| P1 统一入口与项目浏览 | P0；正式 UI 框架和项目存储方案在编码前定案 | A 方向的上游页面/状态设计、GS-Studio 启动与项目浏览、依赖/日志/状态视图 | 设计覆盖正常、等待、失败及恢复；G-01、O-01/03、UI、AC-20/13；实现与运行验收分别记状态 |
| P2 上游流程 GUI | P1；共享业务服务和操作契约 | 地点/场景/Capture、probe/ingest、Run、遮罩审核、RealityScan、repair、分段 QA、恢复与清理界面 | 现行日常 CLI 操作均有 GUI 路径，门禁与清单一致；C-01/02、R-01/02、Q-01/02、AC-17～19 |
| P3 训练与实时检查 | P2；分组、项目定位和训练预设决策 | 合格分段选择、gsplat 默认与 Postshot 可选实验、训练控制、预览和源图对照 | GUI 不改变既有 Run 默认；输入/配置/结果可追溯，故障和恢复不虚报；I、T、V、AC-01～09/15～16 |
| P4 编辑与交付 | P3；拾取语义与快照身份定案 | 独立快照选择、裁剪与变换、撤销重做、项目保存和 PLY 导出 | 原始训练成果受保护，编辑与导出结果可追溯；E、O、AC-10～12 |
| P5 正式验收与发布 | P1～4 实现完成；用户明确恢复相应运行验证 | 对应版本的功能、GUI 尺寸、质量、性能、恢复、离线及互操作证据 | 无未处置 P0 阻断；用户确认 UI 和代表模型画质；AC-01～20 均有有效结论，不继承原型旧报告 |

阶段可以拆分交付，但不能因代码存在、静态设计获认可或旧测试通过而将后续阶段记为已验收。P1 的项目存储；P3 的普通 COLMAP 分组和训练预设；P4 的非穿透拾取规则，仍是进入相应实现前必须记录的决策；UI 技术选型已定为 PySide6（Qt 6），见 [D-12](GS-STUDIO-DECISIONS.md)。质量阈值与代表样本在 P5 前确定，不在本轮猜定。

## 实施进度

实现状态只说明代码已编写并完成静态检查，不代表 CPU/GPU 测试或任何验收场景已通过。

| 阶段 | 实现状态 | 验收状态 |
| --- | --- | --- |
| P0 路线图整合 | 已完成（2026-09-22） | 仅静态核对，未做功能验收 |
| P1 统一入口与项目浏览 | 只读服务与 GUI 壳已扩展为共用写操作层、结构化事件 worker 和操作菜单；尚未运行验收 | 未执行 |
| P2 上游流程 GUI | 创建、探测、外部源重定位、Run 自动阶段推进、Qt 遮罩审核、QA 与清理预览已有代码；中断恢复和实际门禁仍待验证 | 未执行 |
| P3 训练与实时检查 | 合格分段选择、gsplat 默认、Postshot 可选、实际训练日志及每 500 步同视角源图/渲染预览已有代码；交互式训练视角和正式预设仍未完成 | 未执行 |
| P4 编辑与交付 | Qt PLY 查看、独立编辑状态、矩形选择、裁剪、数值变换、撤销重做和新 PLY 导出已有代码；拖拽手柄与正式拾取规则仍未完成 | 未执行 |
| P5 正式验收与发布 | 未开工 | 未执行 |

已定决策见[架构决策记录](GS-STUDIO-DECISIONS.md)（D-05 外部源引用、D-12 UI 技术选型、D-13 服务边界）；D-06、D-07 规则、D-08/D-10 仍待相应阶段决策。2026-09-23 的新增实现只完成静态检查，不继承旧原型测试结论。

## 本轮边界

本轮在 P1 的只读服务上增加共用写操作、Qt 上游流程、实验和编辑路径；未读取或修改真实 Data 素材，未运行 CPU/GPU 测试、应用验收、重建或训练。QA 判定规则与 CLI 默认后端（Postshot Splat ADC）未变。[旧基线](../archive/GS-Studio-Plan/status/BASELINE.md)仅是 2026-09-13 历史证据；当前产品仍待正式验收。
